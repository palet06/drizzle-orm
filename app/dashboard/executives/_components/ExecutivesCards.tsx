//import { db } from "@/configs/db/db";
import React from "react";
//export type ddv = typeof db._.fullSchema.users.$inferSelect;


const ExecutivesCards = () => {
  return (
    <div className="flex flex-col justify-between w-36 h-[250px] border border-black">
      Executive Cards burya gelecek
    </div>
  );
};

export default ExecutivesCards;
